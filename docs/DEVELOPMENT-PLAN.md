# NemoClaw 電路實驗室 — 開發計畫（工業模式擴充）

> 記錄日期：2026-07-23。本文件記錄尚未動工的大型功能規劃：**配電盒（工業配線）模式**與 **PLC 模式**。
> 兩者共用同一套地基，建議按「共用基礎 → 配電盒 → PLC」順序交付。

## 現況摘要（已完成）

- 三種工作模式：自動生成（3D 麵包板）、自由編輯（2D 接線＋ERC）、行為模擬（通電試跑）
- 連通圖引擎：union-find 節點模型，被動元件含直流導通性（電容不導通、二極體方向性、開關可切換）
- LAB AGENT：BYOK Gemini/Gemma function calling，11+ 工具操作全站
- IndexedDB 持久化、元件庫 21 模組＋8 被動元件

---

## 共用地基（兩個工業模式都需要，先做一次）

| 項目 | 內容 | 備註 |
| --- | --- | --- |
| 端子台接線畫布 | 取代麵包板：DIN 軌元件排佈、螺絲端子點對點接線、線號標示 | 沿用 2D 編輯器互動（點兩端子連線） |
| 受控接點求解器 | 「線圈得電 → 接點動作」的定點迭代求解（迭代連通圖直到穩定，偵測震盪） | union-find 每輪重建；自保持／互鎖的核心 |
| 雙迴路電壓域 | 主線路（三相 R/S/T 或 220V）與控制線路（110/220V）分離；跨域接線＝ERC 錯誤 | 取代現有 3V3/5V 模型 |
| 模式切換架構 | 「MCU 沙盒」與「工業配線」為頂層模式，各自持久化 | UI 加頂層切換 |

## 模式一：配電盒（工業配線）— 建議先做

**目標**：台灣工業配線（丙級／乙級）教學情境——傳統電機控制，純接點邏輯、無程式。

### 元件庫
- 電源／保護：NFB 無熔絲開關、栓型保險絲、積熱電驛 TH-RY
- 控制：電磁接觸器 MC（線圈＋主接點×3＋輔助 a/b 接點）、電力電驛 MK、限時電驛 TR（ON-delay）
- 操作／顯示：按鈕 PB（a/b 接點）、選擇開關 COS、指示燈 PL（紅綠黃）、蜂鳴器 BZ
- 負載：三相感應馬達（正逆轉）、單相負載
- 配線材料：端子台 TB、線槽（裝飾性）

### ERC 規則
- 主迴路：NFB → MC 主接點 → TH-RY → 馬達 的串接完整性；缺過載保護＝警告
- 控制迴路：電壓域正確、TH-RY b 接點串入、按鈕 a/b 用法
- 互鎖檢查：正逆轉迴路缺電氣互鎖＝錯誤（會相間短路）
- 短路／未接地／浮接 沿用現有架構

### 模擬（掃描＝定點迭代）
- 按 PB → 迭代求解 → MC 吸持動畫＋咔聲 → 馬達轉動動畫、PL 亮
- 經典考題可跑：自保持啟動/停止、寸動、正逆轉互鎖、Y-Δ 延時啟動（TR 計時）、順序啟動
- 故障注入（進階）：TH-RY 跳脫模擬過載

### 產出物
- 接線表（線號對照）、元件表（BOM）、迴路圖（示意 SVG 匯出）

### Agent 擴充
- 工具：加元件到 DIN 軌、端子接線、通電測試、注入事件（按 PB、過載）
- 知識：提示詞加入配電盒要點；`get_part_info` 涵蓋工業元件

**預估規模**：≈ 自由編輯器＋模擬器的總和（最大單一功能）。可分兩批：
1. 畫布＋元件＋ERC＋「自保持」可模擬
2. 限時電驛、正逆轉/Y-Δ 全套考題、匯出、Agent 工具

## 模式二：PLC — 接在配電盒之後

**目標**：小型 PLC（台達 DVP／三菱 FX 風格，8 DI / 8 DO / 2 AI）放進同一個配電盒，外部元件沿用模式一。

### 新增內容（地基已由模式一提供）
1. PLC 本體元件：DI/DO/AI 端子、24V 電源迴路、COM 共點接法 ERC
2. **梯形圖編輯器**：常開/常閉觸點、線圈、自保持、TON 計時器、計數器（拖放、格線式）
3. 掃描週期模擬：讀輸入 → 執行梯形圖（通電路徑即時亮線）→ 寫輸出；與配電盒求解器串接（PLC DO → MC 線圈）
4. 匯出：IEC 61131-3 結構化文字（ST）＋ IO 對照表（原廠專案檔為封閉格式，不支援）
5. Agent：「幫我做按鈕自保持啟動馬達」→ 自動產生梯形圖＋接線

**方案取捨**（提問時已確認方向為完整版）：
- 方案 A（完整）：圖形化梯形圖編輯器 — 教學價值高，工程量大
- 方案 B（輕量）：規則清單（IF X0 AND NOT X1 THEN Y0）代替梯形圖 — 快但教學效果打折

## 建議交付順序

```
共用地基（端子畫布＋受控接點求解器＋電壓域）
  → 配電盒 批次1（自保持能跑）
  → 配電盒 批次2（全套考題＋匯出）
  → PLC 批次1（本體＋接線＋預設程式模擬）
  → PLC 批次2（梯形圖編輯器＋ST 匯出＋Agent）
```

## 待決事項

- [ ] 配電盒 3D 檢視要不要做（建議先 2D，箱體 3D 後補）
- [ ] 三相電壓表示法（教學簡化 vs 真實相序）
- [ ] 梯形圖持久化格式（自訂 JSON，含註解）
- [ ] 既有 MCU 模式與工業模式之間是否允許混搭（如 ESP32 DO 驅動 MC）——初版不混搭
